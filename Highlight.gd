extends Polygon2D

# func _draw():
	# var points = PoolVector2Array()
	# var color = PoolColorArray()
	# points = [Vector2(x, y), Vector2(x+100, y), Vector2(x+100, y+100), Vector2(x, y+100)]
	# color.append(Color(1.0, 1.0, 1.0, 0.5))
	# draw_polygon(points, color)
